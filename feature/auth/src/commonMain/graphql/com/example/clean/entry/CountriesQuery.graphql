query CountriesQuery {
    countries {
        code
        name
        phone
        emoji
    }
}

query CountryCodeQuery($code: ID!) {
    country(code: $code) {
        code
        name
        phone
        emoji
    }
}
